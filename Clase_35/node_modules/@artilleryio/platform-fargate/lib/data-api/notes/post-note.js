module.exports={postNote:postNote};const debug=require("debug")("data-api:notes:create-note"),createStore=require("../store"),getBackendStore=require("../../utils/get-backend-store");async function postNote(testRunId,text,userId,opts){var store=createStore(await getBackendStore()),store=(await store.init(),await store.addNote(testRunId,text,userId,opts));return store}